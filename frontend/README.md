This folder holds the frontend assets for SAOCLAO.

- `public/`: static assets (CSS, JS, images, uploads) served via `/public`.
- `views/`: EJS templates rendered by the Express backend.

There is no build step; edits here are picked up immediately by the backend server.

<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'SAOCLAO' %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #020202;
      --panel: rgba(20,20,20,0.9);
      --text: #f6f6f6;
      --muted: #c4c4c4;
      --accent: #ffffff;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
      overflow-x: hidden;
    }
    a { color: inherit; text-decoration: none; }
    .stars {
      position: fixed;
      inset: 0;
      z-index: 0;
      pointer-events: none;
      background-image:
        radial-gradient(2px 2px at 20% 30%, rgba(255,255,255,0.8), transparent),
        radial-gradient(2px 2px at 80% 20%, rgba(255,255,255,0.7), transparent),
        radial-gradient(1.5px 1.5px at 50% 70%, rgba(255,255,255,0.6), transparent),
        radial-gradient(2px 2px at 10% 80%, rgba(255,255,255,0.7), transparent);
      animation: twinkle 8s linear infinite;
    }
    @keyframes twinkle {
      0%,100% { opacity: 0.9; }
      50% { opacity: 0.5; }
    }
    .page {
      position: relative;
      z-index: 1;
      padding: 32px 24px 80px;
    }
    .hero {
      max-width: 1100px;
      margin: 0 auto 40px;
      background: linear-gradient(135deg, rgba(255,255,255,0.08), rgba(255,255,255,0.02));
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: 28px;
      padding: 32px;
      position: relative;
      overflow: hidden;
      box-shadow: 0 30px 120px rgba(0,0,0,0.65);
    }
    .hero::after {
      content: '';
      position: absolute;
      inset: 0;
      background: url('https://images.unsplash.com/photo-1497032628192-86f99bcd76bc?w=1600') center/cover;
      opacity: 0.12;
      mix-blend-mode: screen;
      filter: blur(1px);
    }
    .hero-content {
      position: relative;
      display: grid;
      grid-template-columns: 1.1fr 0.9fr;
      align-items: center;
      gap: 24px;
    }
    .logo-row {
      display: flex;
      align-items: center;
      gap: 10px;
      font-weight: 900;
      letter-spacing: -0.5px;
      font-size: 18px;
      margin-bottom: 10px;
    }
    .logo-row img { height: 32px; }
    h1 {
      font-size: clamp(42px, 6vw, 56px);
      margin: 10px 0;
      line-height: 1.05;
      letter-spacing: -1px;
    }
    .subtitle {
      color: var(--muted);
      font-size: 16px;
      margin-bottom: 22px;
      font-weight: 600;
    }
    .cta-row {
      display: flex;
      align-items: center;
      gap: 12px;
      flex-wrap: wrap;
    }
    .btn {
      padding: 12px 20px;
      border-radius: 999px;
      border: 1px solid rgba(255,255,255,0.18);
      background: rgba(255,255,255,0.12);
      color: var(--text);
      font-weight: 800;
      font-size: 14px;
      cursor: pointer;
      transition: transform 0.2s, box-shadow 0.2s, background 0.2s;
    }
    .btn:hover { transform: translateY(-1px); box-shadow: 0 12px 30px rgba(0,0,0,0.35); }
    .btn.primary { background: #fff; color: #000; }
    .btn.ghost { background: rgba(255,255,255,0.08); }
    .search-bar {
      margin-top: 24px;
      background: #fff;
      border-radius: 999px;
      padding: 6px 6px 6px 14px;
      display: flex;
      align-items: center;
      gap: 10px;
      color: #222;
      max-width: 520px;
      box-shadow: 0 12px 30px rgba(0,0,0,0.25);
    }
    .search-bar input {
      border: none;
      outline: none;
      flex: 1;
      padding: 12px 10px;
      font-size: 15px;
      font-weight: 600;
    }
    .hero-figure {
      position: relative;
      justify-self: end;
    }
    .hero-figure img {
      max-height: 320px;
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.5);
    }
    .hero-pill {
      position: absolute;
      bottom: 14px;
      right: 14px;
      background: rgba(0,0,0,0.7);
      color: #fff;
      padding: 8px 12px;
      border-radius: 999px;
      font-weight: 700;
      font-size: 12px;
    }
    .trending {
      max-width: 1100px;
      margin: 0 auto;
      background: rgba(15,15,15,0.9);
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: 22px;
      padding: 28px;
      box-shadow: 0 20px 80px rgba(0,0,0,0.65);
    }
    .trending h2 { margin: 0 0 14px; font-size: 24px; }
    .trending p { margin: 0 0 16px; color: var(--muted); font-weight: 600; }
    .trending .search {
      background: #0d0d0d;
      border: 1px solid rgba(255,255,255,0.12);
      border-radius: 999px;
      padding: 12px 16px;
      display: flex;
      align-items: center;
      gap: 10px;
      color: var(--muted);
    }
    .trending input {
      background: transparent;
      border: none;
      outline: none;
      color: var(--text);
      width: 100%;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="stars"></div>
  <div class="page">
    <section class="hero">
      <div class="hero-content">
        <div>
          <div class="logo-row">
            <img src="/public/img/saoclao.png" alt="SAOCLAO">
            <span>SAOCLAO</span>
          </div>
          <h1>Khám phá.<br>Được khám phá.</h1>
          <div class="subtitle">Nơi nghệ sĩ và người nghe gặp nhau</div>
          <div class="cta-row">
            <a class="btn ghost" href="/login">Đăng nhập</a>
            <a class="btn primary" href="/signup">Đăng ký</a>
          </div>
          <div class="search-bar">
            <i class="fa-solid fa-magnifying-glass" style="color:#555;"></i>
            <input type="text" placeholder="Bắt đầu ngay">
          </div>
        </div>
        <div class="hero-figure">
          <img src="https://i.imgur.com/94ucDyz.png" alt="Hero art">
          <div class="hero-pill">No Beat Battler</div>
        </div>
      </div>
    </section>

    <section class="trending">
      <h2>Hear what's trending for free in the SAOCLAO community</h2>
      <p>Search for artists, bands, tracks, podcasts</p>
      <div class="search">
        <i class="fa-solid fa-magnifying-glass"></i>
        <input type="text" placeholder="Search for artists, bands, tracks, podcasts">
      </div>
    </section>
  </div>
</body>
</html>

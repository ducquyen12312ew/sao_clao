<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Đặt lại mật khẩu - SAOCLAO' %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/public/css/reset-password.css">
</head>
<body>
  <div class="note">♪</div><div class="note">♫</div><div class="note">♪</div><div class="note">♫</div><div class="note">♪</div>

  <header class="header">
    <a class="logo-link" href="/"><img src="/public/img/saoclao.png" alt="SAOCLAO" class="logo-img"></a>
    <a class="btn primary" href="/login">Sign in</a>
  </header>

  <% if (flash) { %>
    <div class="flash <%= flash.type %>"><%= flash.message %></div>
    <script>setTimeout(() => { document.querySelector('.flash')?.classList.add('hide'); }, 2500);</script>
  <% } %>

  <main class="container">
    <div class="auth-card">
      <h2>Đặt lại mật khẩu</h2>
      <p class="subtitle">Tạo mật khẩu mới để tiếp tục.</p>

      <form method="post" action="/reset-password/<%= token %>">
        <div class="input-group">
          <label>Mật khẩu mới</label>
          <input type="password" name="password" required minlength="8" placeholder="Mật khẩu mới" autocomplete="new-password">
          <div class="hint">Ít nhất 8 ký tự, gồm chữ hoa, chữ thường, số và ký tự đặc biệt.</div>
        </div>
        <button class="btn primary" type="submit">Cập nhật mật khẩu</button>
      </form>

      <p class="muted">Nhớ mật khẩu rồi? <a href="/login">Đăng nhập</a></p>
    </div>
  </main>
</body>
</html>

<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Quên mật khẩu - SAOCLAO' %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/public/css/forgot-password.css">
</head>
<body>
  <div class="note">♪</div><div class="note">♫</div><div class="note">♪</div><div class="note">♫</div><div class="note">♪</div>

  <header class="header">
    <a class="logo-link" href="/"><img src="/public/img/saoclao.png" alt="SAOCLAO" class="logo-img"></a>
    <a class="btn primary" href="/login">Sign in</a>
  </header>

  <% if (flash) { %>
    <div class="flash <%= flash.type %>"><%= flash.message %></div>
    <script>setTimeout(() => { document.querySelector('.flash')?.classList.add('hide'); }, 2500);</script>
  <% } %>

  <main class="container">
    <div class="auth-card">
      <h2>Quên mật khẩu?</h2>
      <p class="subtitle">Nhập email để nhận liên kết đặt lại mật khẩu.</p>

      <form method="post" action="/forgot-password">
        <div class="input-group">
          <label>Email</label>
          <input type="email" name="email" required placeholder="you@example.com" autocomplete="email">
        </div>
        <button class="btn primary" type="submit">Gửi liên kết</button>
      </form>

      <p class="muted">Nhớ mật khẩu rồi? <a href="/login">Quay lại đăng nhập</a></p>
    </div>
  </main>
</body>
</html>

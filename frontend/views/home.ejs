<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'SAOCLAO' %></title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
  <style>
    :root {
      --bg: #0a0a0a;
      --panel: #121212;
      --card: #181818;
      --muted: #b3b3b3;
      --text: #f5f5f5;
      --accent: #ff5500;
      --accent-2: #8a5dff;
      --border: #1f1f1f;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      background: var(--bg);
      color: var(--text);
      min-height: 100vh;
    }
    a { color: inherit; text-decoration: none; }
    img { max-width: 100%; display: block; }
    .page {
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    .topbar {
      position: sticky;
      top: 0;
      z-index: 20;
      background: rgba(10,10,10,0.9);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid var(--border);
      padding: 14px 24px;
      display: flex;
      align-items: center;
      gap: 18px;
    }
    .logo {
      display: flex;
      align-items: center;
      gap: 10px;
      font-weight: 900;
      letter-spacing: -0.5px;
    }
    .logo img { height: 28px; }
    .nav {
      display: flex;
      align-items: center;
      gap: 18px;
      font-weight: 700;
      font-size: 14px;
    }
    .nav a {
      padding: 8px 10px;
      border-radius: 8px;
      color: var(--muted);
      transition: color 0.2s, background 0.2s;
    }
    .nav a.active {
      color: var(--text);
      background: rgba(255, 85, 0, 0.12);
    }
    .search {
      flex: 1;
      display: flex;
      align-items: center;
      gap: 10px;
      background: #1a1a1a;
      border: 1px solid var(--border);
      padding: 10px 14px;
      border-radius: 999px;
      max-width: 620px;
    }
    .search input {
      background: transparent;
      border: none;
      outline: none;
      color: var(--text);
      font-size: 14px;
      width: 100%;
    }
    .actions {
      display: flex;
      align-items: center;
      gap: 12px;
    }
    .btn {
      padding: 10px 16px;
      border-radius: 999px;
      border: 1px solid var(--border);
      background: #1a1a1a;
      color: var(--text);
      font-weight: 700;
      font-size: 13px;
      cursor: pointer;
      transition: all 0.2s;
    }
    .btn.primary {
      background: var(--accent);
      border-color: var(--accent);
      color: #0a0a0a;
    }
    .btn.primary:hover { filter: brightness(1.05); }
    .btn.ghost {
      background: transparent;
      color: var(--accent);
      border-color: rgba(255,85,0,0.35);
    }
    .user-avatar {
      width: 34px;
      height: 34px;
      border-radius: 50%;
      background: linear-gradient(135deg, #ff9966, #ff5e62);
      display: grid;
      place-items: center;
      font-weight: 800;
      font-size: 14px;
    }
    .icon-btn {
      width: 36px; height: 36px;
      border-radius: 50%;
      border: 1px solid var(--border);
      background: #1a1a1a;
      color: var(--text);
      display: grid;
      place-items: center;
      cursor: pointer;
      transition: all 0.2s;
    }
    .icon-btn:hover { border-color: rgba(255,85,0,0.35); }
    .avatar-menu { position: relative; }
    .avatar-btn {
      display: flex;
      align-items: center;
      gap: 8px;
      background: transparent;
      border: 1px solid var(--border);
      border-radius: 999px;
      padding: 6px 10px;
      color: var(--text);
      cursor: pointer;
    }
    .avatar-btn i { font-size: 12px; color: var(--muted); }
    .avatar-dropdown {
      position: absolute;
      right: 0;
      top: 44px;
      background: #0f0f0f;
      border: 1px solid var(--border);
      border-radius: 12px;
      min-width: 200px;
      box-shadow: 0 16px 40px rgba(0,0,0,0.45);
      padding: 8px;
      display: none;
      z-index: 30;
    }
    .avatar-dropdown a {
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 10px;
      border-radius: 8px;
      font-weight: 700;
      color: var(--text);
    }
    .avatar-dropdown a:hover {
      background: rgba(255,255,255,0.06);
    }
    .avatar-dropdown i { width: 16px; text-align: center; }

    .more-menu { position: relative; }
    .more-dropdown {
      position: absolute;
      right: 0;
      top: 44px;
      background: #0f0f0f;
      border: 1px solid var(--border);
      border-radius: 12px;
      min-width: 240px;
      box-shadow: 0 16px 40px rgba(0,0,0,0.45);
      padding: 8px 0;
      display: none;
      z-index: 30;
    }
    .more-dropdown a {
      display: block;
      padding: 12px 14px;
      font-weight: 800;
      color: var(--text);
    }
    .more-dropdown a:hover { background: rgba(255,255,255,0.06); }
    .more-section { border-top: 1px solid var(--border); margin-top: 6px; padding-top: 6px; }
    .content {
      display: grid;
      grid-template-columns: 3fr 1.15fr;
      gap: 24px;
      padding: 24px;
    }
    .section {
      margin-bottom: 36px;
    }
    .section-header {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      margin-bottom: 16px;
      gap: 10px;
    }
    .section-title {
      font-size: 20px;
      font-weight: 800;
      letter-spacing: -0.3px;
    }
    .section-sub {
      color: var(--muted);
      font-size: 13px;
      margin-top: 2px;
    }
    .pill-link {
      color: var(--muted);
      font-weight: 600;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.6px;
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
      gap: 16px;
    }
    .row-scroll {
      display: grid;
      grid-auto-flow: column;
      grid-auto-columns: minmax(160px, 1fr);
      gap: 14px;
      overflow-x: auto;
      padding-bottom: 4px;
      scrollbar-width: thin;
    }
    .row-scroll::-webkit-scrollbar { height: 8px; }
    .row-scroll::-webkit-scrollbar-thumb { background: #262626; border-radius: 6px; }
    .card {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 12px;
      transition: transform 0.2s, border-color 0.2s, box-shadow 0.2s;
      display: flex;
      flex-direction: column;
      gap: 10px;
      min-height: 100%;
    }
    .card:hover {
      border-color: rgba(255,85,0,0.35);
      transform: translateY(-2px);
      box-shadow: 0 8px 26px rgba(0,0,0,0.35);
    }
    .cover {
      position: relative;
      width: 100%;
      aspect-ratio: 1;
      border-radius: 12px;
      overflow: hidden;
      background: #222;
    }
    .cover img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    .cover .badge {
      position: absolute;
      top: 10px;
      right: 10px;
      background: rgba(0,0,0,0.7);
      padding: 6px 8px;
      border-radius: 999px;
      font-size: 11px;
      font-weight: 700;
      color: var(--text);
      display: inline-flex;
      align-items: center;
      gap: 6px;
    }
    .title {
      font-weight: 800;
      font-size: 15px;
      margin: 0;
      color: var(--text);
    }
    .meta {
      color: var(--muted);
      font-size: 13px;
      margin: 0;
    }
    .row-two {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
      gap: 16px;
    }
    .sidebar {
      position: sticky;
      top: 82px;
      align-self: start;
      display: flex;
      flex-direction: column;
      gap: 16px;
    }
    .panel {
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 16px;
    }
    .panel h4 {
      margin: 0 0 12px;
      font-size: 14px;
      letter-spacing: 0.5px;
      text-transform: uppercase;
      color: var(--muted);
    }
    .tool-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 10px;
    }
    .tool {
      padding: 12px;
      border: 1px solid var(--border);
      border-radius: 10px;
      background: #1a1a1a;
      display: grid;
      gap: 6px;
      font-weight: 700;
      font-size: 12px;
      color: var(--text);
    }
    .tool i { color: var(--accent); }
    .artist-list {
      display: grid;
      gap: 12px;
    }
    .artist-item {
      display: flex;
      align-items: center;
      gap: 10px;
    }
    .artist-avatar {
      width: 38px;
      height: 38px;
      border-radius: 50%;
      overflow: hidden;
      background: #222;
      display: grid;
      place-items: center;
      font-weight: 700;
    }
    .artist-meta {
      flex: 1;
    }
    .artist-meta .name { font-weight: 700; font-size: 14px; }
    .artist-meta .stats { color: var(--muted); font-size: 12px; }
    .follow-btn {
      padding: 8px 12px;
      border-radius: 999px;
      border: 1px solid var(--border);
      background: #1c1c1c;
      color: var(--text);
      font-weight: 700;
      cursor: pointer;
      transition: all 0.2s;
    }
    .follow-btn:hover {
      border-color: rgba(255,85,0,0.5);
      color: var(--accent);
    }
    .mini-list {
      display: grid;
      gap: 10px;
    }
    .mini-row {
      display: grid;
      grid-template-columns: auto 1fr;
      gap: 10px;
      align-items: center;
    }
    .mini-thumb {
      width: 48px;
      height: 48px;
      border-radius: 10px;
      overflow: hidden;
      background: #222;
    }
    .section-accent {
      background: linear-gradient(135deg, rgba(255,85,0,0.16), rgba(255,85,0,0.04));
      border: 1px solid rgba(255,85,0,0.35);
    }
    @media (max-width: 1100px) {
      .content { grid-template-columns: 1fr; }
      .sidebar { position: relative; top: 0; }
    }
    @media (max-width: 768px) {
      .topbar { flex-wrap: wrap; gap: 10px; }
      .nav { width: 100%; order: 3; }
      .search { order: 2; }
      .actions { order: 4; width: 100%; justify-content: flex-end; }
      .content { padding: 16px; }
    }
  </style>
</head>
<body>
  <% 
    const currentUser = typeof user !== 'undefined' ? user : null;
    const incomingTracks = (typeof tracks !== 'undefined' && Array.isArray(tracks)) ? tracks : [];
  %>
  <div class="page">
    <header class="topbar">
      <a class="logo" href="/home">
        <img src="/public/img/saoclao.png" alt="SAOCLAO">
        <span>SAOCLAO</span>
      </a>
      <nav class="nav">
        <a href="/home" class="active">Home</a>
        <a href="/feed">Feed</a>
        <a href="/library">Library</a>
      </nav>
      <div class="search">
        <i class="fa-solid fa-magnifying-glass" style="color:var(--muted);"></i>
        <input type="text" placeholder="Search for artists, tracks, podcasts">
      </div>
      <div class="actions">
        <button class="icon-btn" title="Messages"><i class="fa-regular fa-envelope"></i></button>
        <button class="icon-btn" title="Notifications"><i class="fa-regular fa-bell"></i></button>
        <div class="more-menu">
          <button class="icon-btn" id="moreBtn" title="More options"><i class="fa-solid fa-ellipsis"></i></button>
          <div class="more-dropdown" id="moreDropdown">
            <a href="/about">About us</a>
            <a href="/legal">Legal</a>
            <a href="/copyright">Copyright</a>
            <div class="more-section">
              <a href="/apps">Mobile apps</a>
              <a href="/creators">For Creators</a>
              <a href="/newsroom">Newsroom</a>
              <a href="/jobs">Jobs</a>
              <a href="/developers">Developers</a>
              <a href="/store">SAOCLAO Store</a>
            </div>
            <div class="more-section">
              <a href="/support">Support</a>
              <a href="/shortcuts">Keyboard shortcuts</a>
            </div>
            <div class="more-section">
              <a href="/subscription">Subscription</a>
              <a href="/settings">Settings</a>
              <a href="/logout">Sign out</a>
            </div>
          </div>
        </div>
        <a href="/upload" class="btn">Upload</a>
        <a href="/artistpro" class="btn ghost">Try Artist Pro</a>
        <div class="avatar-menu">
          <button class="avatar-btn" id="avatarBtn">
            <div class="user-avatar"><%= (currentUser && currentUser.username ? currentUser.username.charAt(0).toUpperCase() : 'S') %></div>
            <i class="fa-solid fa-chevron-down"></i>
          </button>
          <div class="avatar-dropdown" id="avatarDropdown">
            <a href="<%= currentUser ? '/users/' + currentUser.username : '/login' %>"><i class="fa-regular fa-user"></i> Profile</a>
            <a href="/likes"><i class="fa-regular fa-heart"></i> Likes</a>
            <a href="/playlists"><i class="fa-solid fa-compact-disc"></i> Playlists</a>
            <a href="/library#stations"><i class="fa-solid fa-tower-broadcast"></i> Stations</a>
            <a href="/library#following"><i class="fa-regular fa-user"></i> Following</a>
            <a href="/feed"><i class="fa-solid fa-user-plus"></i> Who to follow</a>
            <a href="/artistpro"><i class="fa-solid fa-star"></i> Try Artist Pro</a>
            <a href="/home"><i class="fa-solid fa-chart-column"></i> Insights</a>
            <a href="/upload"><i class="fa-solid fa-sliders"></i> Tracks</a>
            <a href="/upload"><i class="fa-solid fa-arrows-rotate"></i> Distribute</a>
          </div>
        </div>
      </div>
    </header>
<%
  const sourceTracks = (incomingTracks.length ? incomingTracks : [
    { title: 'Midnight Echoes', artist: 'SAOCLAO Originals', coverUrl: 'https://images.unsplash.com/photo-1511379938547-c1f69419868d?w=600' },
    { title: 'Attention Remix', artist: 'Charlie Puth', coverUrl: 'https://images.unsplash.com/photo-1511671782779-c97d3d27a1d4?w=600' },
    { title: 'Year In Review', artist: 'SAOCLAO Stories', coverUrl: 'https://images.unsplash.com/photo-1519677100203-a0e668c92439?w=600' },
    { title: 'Neon Nights', artist: 'Luna Rivers', coverUrl: 'https://images.unsplash.com/photo-1470229538611-16ba8c7ffbd7?w=600' },
    { title: 'Retrograde', artist: 'Nova Pulse', coverUrl: 'https://images.unsplash.com/photo-1421809313281-48f03fa45e9f?w=600' },
    { title: 'City Lights', artist: 'Orion Keys', coverUrl: 'https://images.unsplash.com/photo-1494232410401-ad00d5433cfa?w=600' },
    { title: 'Daydream', artist: 'Echo Bloom', coverUrl: 'https://images.unsplash.com/photo-1483412033650-1015ddeb83d1?w=600' },
    { title: 'Afterglow', artist: 'Night Tempo', coverUrl: 'https://images.unsplash.com/photo-1483412033650-1015ddeb83d1?w=600' }
  ]);
  const recentTracks = sourceTracks.slice(0, 6);
  const playbackTracks = sourceTracks.slice(0, 5);
  const likeTracks = sourceTracks.slice(1, 7);
  const mixedTracks = sourceTracks.slice(2, 8);
  const albumTracks = sourceTracks.slice(0, 6);
  const stationTracks = sourceTracks.slice(3, 7);
  const curatedTracks = sourceTracks.slice(0, 4);
  const spotlightArtists = sourceTracks.slice(4, 8);
%>
    <main class="content">
      <div>
        <section class="section">
          <div class="section-header">
            <div>
              <div class="section-title">Recently Played</div>
              <div class="section-sub">Jump back into what you had on repeat</div>
            </div>
            <a class="pill-link" href="/history">View all</a>
          </div>
          <div class="row-two">
            <% recentTracks.slice(0,2).forEach(track => { %>
              <a class="card" href="<%= track._id ? '/track/' + track._id : '#' %>">
                <div class="cover" style="border-radius: 50%;">
                  <img src="<%= track.coverUrl || 'https://images.unsplash.com/photo-1511379938547-c1f69419868d?w=600' %>" alt="<%= track.title %>">
                  <div class="badge"><i class="fa-solid fa-check" style="color:#3dd598;"></i> Verified</div>
                </div>
                <div class="title"><%= track.title %></div>
                <p class="meta"><%= track.artist || 'Unknown artist' %></p>
              </a>
            <% }) %>
            <% recentTracks.slice(2).forEach(track => { %>
              <a class="card" href="<%= track._id ? '/track/' + track._id : '#' %>">
                <div class="cover">
                  <img src="<%= track.coverUrl || 'https://images.unsplash.com/photo-1511379938547-c1f69419868d?w=600' %>" alt="<%= track.title %>">
                </div>
                <div class="title"><%= track.title %></div>
                <p class="meta"><%= track.artist || 'Unknown artist' %></p>
              </a>
            <% }) %>
          </div>
        </section>

        <section class="section">
          <div class="section-header">
            <div>
              <div class="section-title">Play it back</div>
              <div class="section-sub">Relive the tracks that defined your year</div>
            </div>
          </div>
          <div class="row-scroll">
            <% playbackTracks.forEach((track, idx) => { %>
              <a class="card" href="<%= track._id ? '/track/' + track._id : '#' %>">
                <div class="cover">
                  <img src="<%= track.coverUrl %>" alt="<%= track.title %>">
                  <div class="badge"><i class="fa-solid fa-music"></i> <%= 2025 - idx %> Playback</div>
                </div>
                <div class="title"><%= track.title %></div>
                <p class="meta">Made for you</p>
              </a>
            <% }) %>
          </div>
        </section>

        <section class="section">
          <div class="section-header">
            <div>
              <div class="section-title">More of what you like</div>
              <div class="section-sub">Fresh picks based on your listening</div>
            </div>
          </div>
          <div class="grid">
            <% likeTracks.forEach(track => { %>
              <a class="card" href="<%= track._id ? '/track/' + track._id : '#' %>">
                <div class="cover">
                  <img src="<%= track.coverUrl %>" alt="<%= track.title %>">
                </div>
                <div class="title"><%= track.title %></div>
                <p class="meta"><%= track.artist || 'Unknown artist' %></p>
              </a>
            <% }) %>
          </div>
        </section>

        <section class="section">
          <div class="section-header">
            <div>
              <div class="section-title">Mixed for you</div>
              <div class="section-sub">Hand-tuned blends for every mood</div>
            </div>
          </div>
          <div class="row-scroll">
            <% mixedTracks.forEach(track => { %>
              <a class="card section-accent" href="<%= track._id ? '/track/' + track._id : '#' %>">
                <div class="cover">
                  <img src="<%= track.coverUrl %>" alt="<%= track.title %>">
                </div>
                <div class="title"><%= track.title %></div>
                <p class="meta">Mix • <%= track.artist || 'SAOCLAO' %></p>
              </a>
            <% }) %>
          </div>
        </section>

        <section class="section">
          <div class="section-header">
            <div>
              <div class="section-title">Albums for you</div>
              <div class="section-sub">Full projects you will vibe with</div>
            </div>
          </div>
          <div class="grid">
            <% albumTracks.forEach(track => { %>
              <a class="card" href="<%= track._id ? '/track/' + track._id : '#' %>">
                <div class="cover">
                  <img src="<%= track.coverUrl %>" alt="<%= track.title %>">
                  <div class="badge"><i class="fa-solid fa-record-vinyl"></i> Album</div>
                </div>
                <div class="title"><%= track.title %></div>
                <p class="meta"><%= track.artist || 'Unknown artist' %></p>
              </a>
            <% }) %>
          </div>
        </section>

        <section class="section">
          <div class="section-header">
            <div>
              <div class="section-title">Discover with station</div>
              <div class="section-sub">Start a station to stay in the zone</div>
            </div>
          </div>
          <div class="row-scroll">
            <% stationTracks.forEach(track => { %>
              <a class="card" href="<%= track._id ? '/track/' + track._id : '#' %>">
                <div class="cover">
                  <img src="<%= track.coverUrl %>" alt="<%= track.title %>">
                  <div class="badge"><i class="fa-solid fa-signal"></i> Station</div>
                </div>
                <div class="title"><%= track.title %></div>
                <p class="meta"><%= track.artist || 'Unknown artist' %></p>
              </a>
            <% }) %>
          </div>
        </section>

        <section class="section">
          <div class="section-header">
            <div>
              <div class="section-title">Curated by SAOCLAO</div>
              <div class="section-sub">Editor picks just for you</div>
            </div>
          </div>
          <div class="row-scroll">
            <% curatedTracks.forEach(track => { %>
              <a class="card" href="<%= track._id ? '/track/' + track._id : '#' %>">
                <div class="cover">
                  <img src="<%= track.coverUrl %>" alt="<%= track.title %>">
                  <div class="badge"><i class="fa-solid fa-star"></i> Pick</div>
                </div>
                <div class="title"><%= track.title %></div>
                <p class="meta"><%= track.artist || 'Unknown artist' %></p>
              </a>
            <% }) %>
          </div>
        </section>

        <section class="section">
          <div class="section-header">
            <div>
              <div class="section-title">Artist to watch out for</div>
              <div class="section-sub">Up-and-coming talent on SAOCLAO</div>
            </div>
          </div>
          <div class="row-two">
            <% spotlightArtists.forEach(artist => { %>
              <div class="card">
                <div class="cover" style="border-radius: 50%;">
                  <img src="<%= artist.coverUrl %>" alt="<%= artist.artist %>">
                  <div class="badge"><i class="fa-solid fa-bolt"></i> Rising</div>
                </div>
                <div class="title"><%= artist.artist || 'New Artist' %></div>
                <p class="meta">Featured on SAOCLAO</p>
                <button class="follow-btn" style="width: fit-content;">Follow</button>
              </div>
            <% }) %>
          </div>
        </section>
      </div>

      <aside class="sidebar">
        <div class="panel">
          <h4>Artist tools</h4>
          <div class="tool-grid">
            <div class="tool"><i class="fa-solid fa-bullhorn"></i> Amplify</div>
            <div class="tool"><i class="fa-solid fa-arrow-rotate-right"></i> Replace</div>
            <div class="tool"><i class="fa-solid fa-globe"></i> Distribute</div>
            <div class="tool"><i class="fa-solid fa-wand-magic-sparkles"></i> Master</div>
          </div>
          <div style="margin-top:12px; font-size:12px; color:var(--muted);">Unlock artist tools from $4.00/month.</div>
        </div>

        <div class="panel">
          <h4>Artists you should follow</h4>
          <div class="artist-list">
            <% likeTracks.slice(0,4).forEach(artist => { %>
              <div class="artist-item">
                <div class="artist-avatar">
                  <% if (artist.coverUrl) { %>
                    <img src="<%= artist.coverUrl %>" alt="<%= artist.artist %>">
                  <% } else { %>
                    <%= (artist.artist || 'S')[0] %>
                  <% } %>
                </div>
                <div class="artist-meta">
                  <div class="name"><%= artist.artist || 'Artist' %></div>
                  <div class="stats">Followers · Trending</div>
                </div>
                <button class="follow-btn">Follow</button>
              </div>
            <% }) %>
          </div>
        </div>

        <div class="panel">
          <h4>Recent likes</h4>
          <div class="mini-list">
            <% recentTracks.slice(0,4).forEach(track => { %>
              <div class="mini-row">
                <div class="mini-thumb">
                  <img src="<%= track.coverUrl %>" alt="<%= track.title %>">
                </div>
                <div>
                  <div class="title" style="font-size:13px;"><%= track.title %></div>
                  <div class="meta"><%= track.artist || 'Unknown artist' %></div>
                </div>
              </div>
            <% }) %>
          </div>
        </div>
      </aside>
    </main>
  </div>

  <script>
    const avatarBtn = document.getElementById('avatarBtn');
    const avatarDropdown = document.getElementById('avatarDropdown');
    const moreBtn = document.getElementById('moreBtn');
    const moreDropdown = document.getElementById('moreDropdown');

    const closeMenus = (e) => {
      if (avatarDropdown && !avatarDropdown.contains(e.target) && !avatarBtn.contains(e.target)) {
        avatarDropdown.style.display = 'none';
      }
      if (moreDropdown && !moreDropdown.contains(e.target) && !moreBtn.contains(e.target)) {
        moreDropdown.style.display = 'none';
      }
    };

    avatarBtn?.addEventListener('click', (e) => {
      e.stopPropagation();
      if (avatarDropdown) {
        const isOpen = avatarDropdown.style.display === 'block';
        avatarDropdown.style.display = isOpen ? 'none' : 'block';
        if (moreDropdown) moreDropdown.style.display = 'none';
      }
    });

    moreBtn?.addEventListener('click', (e) => {
      e.stopPropagation();
      if (moreDropdown) {
        const isOpen = moreDropdown.style.display === 'block';
        moreDropdown.style.display = isOpen ? 'none' : 'block';
        if (avatarDropdown) avatarDropdown.style.display = 'none';
      }
    });

    document.addEventListener('click', closeMenus);
  </script>
</body>
</html>
